# Copy this file to `.env.local` and fill in a valid Desmos API key for local
# development. The key is read at build time by Vite and inlined into the client
# bundle, so it is visible to anyone who opens the app — see README for details.
VITE_DESMOS_API_KEY=
